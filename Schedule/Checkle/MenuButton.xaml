<UserControl x:Class="Checkle.MenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Checkle"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="55">
    <Grid>
        <Rectangle HorizontalAlignment="Right" Margin="0,0,0,0" Width="3" Fill="#7FF3F3F3"/>
        <TextBlock x:Name="tb" MouseEnter="tb_MouseEnter" MouseLeave="tb_MouseLeave" Padding="0,2,0,0" TextWrapping="Wrap" Text="RESET" Opacity="0.5" TextAlignment="Center" Foreground="#CCFFFFFF" FontFamily="/Checkle;component/Fonts/#HangeulNuri" FontSize="15"/>

    </Grid>
</UserControl>
